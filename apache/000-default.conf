<VirtualHost *:8080>
    ServerAdmin tombo@localhost

    DocumentRoot /var/www/html

    ErrorLog ${APACHE_LOG_DIR}/error.log

    LoadModule rewrite_module /usr/lib/apache2/modules/mod_rewrite.so

    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>